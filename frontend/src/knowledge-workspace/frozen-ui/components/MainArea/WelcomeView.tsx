import React from 'react';
import { Database, Search } from 'lucide-react';

export default function WelcomeView({ searchParams, setSearchParams }: any) {
  // Empty render because ChatAssistant handles the Home Chat logic now when isHomeChat is true
  return null;
}