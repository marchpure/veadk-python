# Semantic Skill HTML bundle

Use this bundle for schema discovery and metric contracts. Put entities and
field roles first, then verified joins, lineage and the MDL source as a
secondary disclosure. Ambiguities and dependency errors are never silently
resolved; they become visible review callouts.
