# Semantic acceptance checklist

- [ ] Entity, field role, aggregation, unit and source field are visible.
- [ ] Join direction, type and evidence locator are visible.
- [ ] Metric/dimension lineage and review errors are visible.
- [ ] MDL is secondary, escaped and disclosure-only.
- [ ] No JSON `<pre>` is the primary view; no scripts or external requests.
