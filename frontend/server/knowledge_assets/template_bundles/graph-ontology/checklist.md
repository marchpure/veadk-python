# Graph acceptance checklist

- [ ] Nodes, edges, types, legend and overview counts are visible.
- [ ] SVG is deterministic, labeled and keyboard-selectable.
- [ ] Relation evidence and conflicts are visible beside the map.
- [ ] Filter/select/refresh are declarative artifact events.
- [ ] No JSON `<pre>` primary view, script, iframe or external network.
